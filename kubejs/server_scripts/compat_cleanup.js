// =============================================================================
// COMPAT / CLEANUP
// Intentionally near-empty. Gating removals live with their replacements in
// the chapter files (no dead ends — see docs/CONVENTIONS.md). This file is
// where playtest-discovered recipe conflicts and JEI clutter fixes go, so the
// chapter files stay pure progression.
// =============================================================================

ServerEvents.recipes(event => {
	// (nothing yet — first playtest will populate this)
})
